function playAudioBg(){
     var randomNb = Math.floor((Math.random() * 3));
    if(randomNb == 0)
    {   audio_bg_roof.load();
          audio_bg_roof.play(); 
        audio_bg_roof.currentTime = 0;
 
    }else if(randomNb == 1)
    {   audio_bg_roof.load();
          audio_bg_roof.play(); 
        audio_bg_roof.currentTime = 0;
     
    }else if(randomNb == 2)
    {   audio_bg_roof.load();
          audio_bg_roof.play(); 
        audio_bg_roof.currentTime = 0;
 
    }
}