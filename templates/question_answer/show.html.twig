{% extends 'home.html.twig' %}

{% block title %}QuestionAnswer{% endblock %}

{% block body %}
    <h1>QuestionAnswer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_answer.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question_answer.question }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ question_answer.answer }}</td>
            </tr>
            <tr>
                <th>Imgpath</th>
                <td>{{ question_answer.imgpath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('question_answer_index', {'domain': question_answer.domain}) }}">back to list</a>

    <a href="{{ path('question_answer_edit', {'id': question_answer.id}) }}">edit</a>

    {{ include('question_answer/_delete_form.html.twig') }}
{% endblock %}
